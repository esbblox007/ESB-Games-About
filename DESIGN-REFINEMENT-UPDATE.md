# ESB Games design refinement update

This update contains the requested visual refinements:

- Larger “Worlds built in minutes, not months.” heading without resizing the card.
- Purple CTA gradients now begin cleanly in purple, removing the magenta/red-looking left edge.
- Footer content is constrained and grouped more closely on every page.
- The blue ESB logo replaces the gradient E mark in the header and footer.
- The home hero now uses a much larger circular glow with all three animated activity cards retained on the right.

Upload the extracted `app`, `components`, and `public` folders to the repository root and commit the replacements to `main`.
