# QA fix — Careers uploads and homepage CTA contrast

- Restores trusted `service_role` CRUD privileges for the private `candidate_uploads` registry while revoking public-role access.
- Cleans up private Storage objects if upload metadata registration fails.
- Keeps the recruitment candidate bucket private and the existing 15 MB/MIME validation unchanged.
- Forces the pre-launch homepage `View News & Updates` primary CTA to high-contrast white text across normal, visited, hover and keyboard-focus states.

Production database permission fix was applied and verified with a transaction-scoped `service_role` insert before this code was merged.
